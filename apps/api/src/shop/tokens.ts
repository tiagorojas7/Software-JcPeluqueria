/** DI tokens for this module's port dependencies — one token per module,
 *  never reused across modules, same pattern every other feature module in
 *  this app follows. */
export const SHOP_REVENUE_REPOSITORY = Symbol('SHOP_REVENUE_REPOSITORY');
export const CLOCK = Symbol('SHOP_CLOCK');
