schema: spec-driven

context: |
  Project: JC Barbería MVP web app (real barbershop, Córdoba Capital, Argentina).
  Status: Phase 0 (Fundación) implemented on feat/turnero-00-fundacion.
  Tech stack: NestJS + React/Vite + PostgreSQL + Drizzle + pg-boss, TypeScript
  monorepo (pnpm workspaces) with hexagonal/screaming architecture. See design.md.
  Architecture: apps/{api,worker,web} + packages/{domain,application,infrastructure,contracts}.
  Testing: Vitest (root `pnpm test` runs every package's suite recursively).
  Persistence: hybrid (Engram + OpenSpec).

strict_tdd: true
strict_tdd_note: >
  User's global preference (~/.claude/CLAUDE.md) sets Strict TDD Mode: enabled.
  Flipped to true in Phase 0 (task 0.6) now that Vitest exists as the test
  runner across the monorepo. sanity.spec.ts and ShopClock.businessDayBounds
  were both written RED-first as the establishing evidence.

rules:
  proposal:
    - Include rollback plan for risky changes
    - Keep the proposal at intent/scope/approach altitude; it may state constraints
      the stack must satisfy, but stack and architecture selection belongs to sdd-design
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
    - Respect the 400-changed-line review budget; recommend chained PRs when at risk
  apply:
    guidelines:
      - Follow existing code patterns once a stack exists
    tdd: true
    test_command: "pnpm test"
  verify:
    test_command: "pnpm test"
    build_command: "pnpm typecheck && pnpm lint && pnpm depcruise"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
